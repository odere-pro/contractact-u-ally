// Audience copy. The product is shipped for migrant workers; the
// previously interactive multi-profile selector has been retired.
// These two strings are the only audience-facing labels still rendered
// — the file is kept under its original name to minimise import churn
// and can be renamed to `audience.ts` in a follow-up.

export const MIGRANT_WORKER_LABEL = "Migrant Worker";
export const MIGRANT_WORKER_TAGLINE =
  "Plain-language explanation, focused on what to do next and who can help.";
